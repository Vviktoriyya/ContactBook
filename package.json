{
  "name": "front-end",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "npx tailwindcss -i ./src/styles/input.css -o ./src/styles/output.css --watch --content ./src/**/*.{html,js} & npx live-server --root=./src/pages --open=index.html"
  },

  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.0",
    "live-server": "^1.2.2",
    "tailwindcss": "^4.1.11"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.11"
  }
}
